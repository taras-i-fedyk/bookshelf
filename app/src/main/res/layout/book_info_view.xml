<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="com.tarasfedyk.example.bookshelf.biz.models.BookInfo" />
        <import type="com.tarasfedyk.example.bookshelf.ui.adapters.BookInfosAdapter.OnItemClickListener" />

        <variable name="item" type="BookInfo" />
        <variable name="onItemClickListener" type="OnItemClickListener" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?android:attr/selectableItemBackground">

        <TextView
            android:id="@+id/title_view"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            android:layout_margin="16dp"
            style="@style/MainTextAppearance"
            android:text="@{item.title}"
            android:onClick="@{() -> onItemClickListener.onItemClicked(item)}" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>
